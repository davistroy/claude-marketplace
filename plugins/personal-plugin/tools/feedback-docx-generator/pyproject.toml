[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "feedback-docx-generator"
version = "1.0.0"
description = "Generate professional feedback assessment .docx documents from structured JSON input"
license = {text = "MIT"}
requires-python = ">=3.10"
keywords = ["feedback", "assessment", "docx", "document-generation"]
dependencies = [
    "python-docx>=1.0.0",
]

[project.scripts]
feedback-docx-generator = "feedback_docx_generator.__main__:main"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "pytest-xdist>=3.5",
    "mypy>=1.8.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "--cov=feedback_docx_generator --cov-report=term-missing"

[tool.coverage.run]
source = ["src/feedback_docx_generator"]
branch = true

[tool.coverage.report]
fail_under = 95

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
